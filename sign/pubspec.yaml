name: sign
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  image_picker: ^1.0.5
  camera: ^0.10.5+5
  video_player: ^2.8.1
  path: ^1.8.3
  speech_to_text: ^7.3.0
  path_provider: ^2.0.14
  flutter_tts: ^3.8.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  flutter_launcher_icons: ^0.14.4   # ✅ Correct version

flutter:
  uses-material-design: true
  assets:
    - assets/sign.webp

flutter_icons:   # ✅ Correct placement (root level, same as flutter:)
  android: true
  ios: true
  image_path: "assets/sign.webp"
